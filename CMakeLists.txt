# Root CMakeLists.txt for ESP-IDF Project
cmake_minimum_required(VERSION 3.16)

# Include the standard ESP-IDF project tools
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

# Name the project
project(Black-Flipper-firmware)
